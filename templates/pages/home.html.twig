{% extends 'base.html.twig' %}

{% block title 'Agence Loren Ipsum' %}

{%  block body  %}
   <div> class='jumbotron text-centre'>
<h1> Agence loren ipsum </h1>
    <p> loren ipsum do sir amet un element real </p>
    </div>
    <div class="container">
    </div>
      <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
            <div class="col-3">
                <div class="card">
                 <div class=".card-body" >
                     <h5 class="card-title">
                         <a href="{{ path('property.show'),{id:property.id,slu: property.slug }}">{{ property.title }}</a>
                     </h5>
                     <p class="card-text">{{ property.city }} {{ property.postalCode }}</p>
                     <div class="text-primary" style="font-weight: bold;font-size: 2rem;"{{ property.formattedPrice }} £</div>
                 </div>
                </div>
            </div>
            {% endfor %}
        </div>
{% endblock %}


